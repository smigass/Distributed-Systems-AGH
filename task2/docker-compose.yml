services:
  radar:
    build:
      context: .
      dockerfile: radar/Dockerfile
    ports:
      - "8080:8080"
    environment:
      - CLIENT_ID=${CLIENT_ID}
      - CLIENT_SECRET=${CLIENT_SECRET}
      - AEROBOX_API_KEY=${AEROBOX_API_KEY}
      - STACK_API_KEY=${STACK_API_KEY}
